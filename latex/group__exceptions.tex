\hypertarget{group__exceptions}{}\doxysection{An execption-\/like library.}
\label{group__exceptions}\index{An execption-\/like library.@{An execption-\/like library.}}


An exception-\/like mechanism for C.  


Collaboration diagram for An execption-\/like library.\+:
\nopagebreak
\begin{figure}[H]
\begin{center}
\leavevmode
\includegraphics[width=350pt]{group__exceptions}
\end{center}
\end{figure}
\doxysubsection*{Modules}
\begin{DoxyCompactItemize}
\item 
\mbox{\hyperlink{group__helpers}{Helpers for exceptions}}
\begin{DoxyCompactList}\small\item\em These are some internal helpers, not part of the public A\+PI. \end{DoxyCompactList}\end{DoxyCompactItemize}
\doxysubsection*{Classes}
\begin{DoxyCompactItemize}
\item 
struct \mbox{\hyperlink{structexception__handler__frame}{exception\+\_\+handler\+\_\+frame}}
\item 
struct \mbox{\hyperlink{structexception__stack__frame}{exception\+\_\+stack\+\_\+frame}}
\end{DoxyCompactItemize}
\doxysubsection*{Macros}
\begin{DoxyCompactItemize}
\item 
\mbox{\Hypertarget{group__exceptions_gadf8cc03bd0ede7f3e5aafa57de5016dd}\label{group__exceptions_gadf8cc03bd0ede7f3e5aafa57de5016dd}} 
\#define {\bfseries \+\_\+\+\_\+concatenate\+\_\+tokens}(x,  y)~x \#\# y
\item 
\mbox{\Hypertarget{group__exceptions_ga0d2e02c74b05d3ff3bfa9eb3dcd891e3}\label{group__exceptions_ga0d2e02c74b05d3ff3bfa9eb3dcd891e3}} 
\#define {\bfseries \+\_\+\+\_\+conc}(z,  w)~\+\_\+\+\_\+concatenate\+\_\+tokens(z,w)
\end{DoxyCompactItemize}
\doxysubsection*{Typedefs}
\begin{DoxyCompactItemize}
\item 
\mbox{\Hypertarget{group__exceptions_ga07c60bc46505779b049ce597fe609258}\label{group__exceptions_ga07c60bc46505779b049ce597fe609258}} 
typedef void($\ast$ {\bfseries exception\+\_\+handler}) (int)
\item 
\mbox{\Hypertarget{group__exceptions_ga644495f4913e96a83cdf2779fd813974}\label{group__exceptions_ga644495f4913e96a83cdf2779fd813974}} 
typedef struct \mbox{\hyperlink{structexception__stack__frame}{exception\+\_\+stack\+\_\+frame}} $\ast$$\ast$ {\bfseries exception\+\_\+context}
\end{DoxyCompactItemize}
\doxysubsection*{Functions}
\begin{DoxyCompactItemize}
\item 
\mbox{\Hypertarget{group__exceptions_ga7eda2653ee54e0c95e9d51b8fd873298}\label{group__exceptions_ga7eda2653ee54e0c95e9d51b8fd873298}} 
void {\bfseries raise\+\_\+exception} (\mbox{\hyperlink{structexception__stack__frame}{exception\+\_\+context}} context)
\item 
\mbox{\Hypertarget{group__exceptions_ga5232fee2c414c5ff1ff44dc28c83cf42}\label{group__exceptions_ga5232fee2c414c5ff1ff44dc28c83cf42}} 
void {\bfseries exception\+\_\+unwind} (\mbox{\hyperlink{structexception__stack__frame}{exception\+\_\+context}} context, int errcode)
\end{DoxyCompactItemize}


\doxysubsection{Detailed Description}
An exception-\/like mechanism for C. 

Exceptions are supported by many object-\/oriented languages, such as Java, C++ and Python, but not by C. This makes programming certain kinds of tasks somewhat complicated. These are tasks that can cause a thread to exit back through several layers of calls. For example, a system call may lead to a stack of nested calls to execute the code of a driver. If processing is to be rolled back to the point of entry to the kernel, all calls in that stack need to propagate the error. This makes coding tedious and error-\/prone.

In C, there is a standard-\/library facility that can be used to implement such functionality, available by including {\ttfamily $<$setjmp.\+h$>$}. The help is in the form of functions {\ttfamily setjmp()} and {\ttfamily longjmp} (and their vatiatons). In this library, these standard calls, wrapped by some suitable macros, and using some G\+NU G\+CC extensions (nested functions), provide some easy-\/to-\/use exception-\/like programming structures.\hypertarget{group__exceptions_autotoc_md27}{}\doxysubsubsection{Examples}\label{group__exceptions_autotoc_md27}
Before we describe the details, we show some examples of the library\textquotesingle{}s use.

A try-\/block is declared as follows\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{TRY\_WITH(context) \{}
\DoxyCodeLine{}
\DoxyCodeLine{    ON\_ERROR \{}
\DoxyCodeLine{        printf(\textcolor{stringliteral}{"Error in what I was doing\(\backslash\)n"});}
\DoxyCodeLine{        \textcolor{comment}{// After this, execute the finally }}
\DoxyCodeLine{    \}}
\DoxyCodeLine{}
\DoxyCodeLine{    FINALLY(e) \{}
\DoxyCodeLine{        \textcolor{keywordflow}{if}(e) }
\DoxyCodeLine{            printf(\textcolor{stringliteral}{"Continuing after error\(\backslash\)n"});         }
\DoxyCodeLine{        \textcolor{keywordflow}{else}}
\DoxyCodeLine{            printf(\textcolor{stringliteral}{"Finished without error\(\backslash\)n"});}
\DoxyCodeLine{    \}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// do something here }}
\DoxyCodeLine{    \textcolor{keywordflow}{if}(error\_happens)}
\DoxyCodeLine{        raise\_exception(context);}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// or call a function that may call raise\_exception()}}
\DoxyCodeLine{    do\_something\_else();}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// If we leave here, FINALLY will be executed }}
\DoxyCodeLine{\}}
\end{DoxyCode}


For example, one could do the following, to construct a composite resource\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{Resource r1, r2, r3;}
\DoxyCodeLine{TRY\_WITH(context) \{}
\DoxyCodeLine{    lock\_monitor();}
\DoxyCodeLine{    FINALLY(e) \{}
\DoxyCodeLine{        unlock\_monitor();}
\DoxyCodeLine{    \}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// This may raise\_exception(...)}}
\DoxyCodeLine{    r1 = acquire\_resource1();}
\DoxyCodeLine{    ON\_ERROR \{}
\DoxyCodeLine{        release\_resource1(r1);}
\DoxyCodeLine{    \}}
\DoxyCodeLine{}
\DoxyCodeLine{    \textcolor{comment}{// This may raise\_exception(...)}}
\DoxyCodeLine{    r2 = acquire\_resource2(r2);}
\DoxyCodeLine{}
\DoxyCodeLine{    ON\_ERROR \{}
\DoxyCodeLine{        release\_resource2(r2);}
\DoxyCodeLine{    \}}
\DoxyCodeLine{    }
\DoxyCodeLine{    \textcolor{comment}{// This may raise\_exception(...)}}
\DoxyCodeLine{    r3 = acquire\_resrouce3(r1, r2);}
\DoxyCodeLine{\}}
\DoxyCodeLine{\textcolor{keywordflow}{return} r3;}
\end{DoxyCode}
\hypertarget{group__exceptions_autotoc_md28}{}\doxysubsubsection{How it works}\label{group__exceptions_autotoc_md28}
The workings are based on the idea of an {\bfseries{exception stack}}. The elements of this stack are called {\bfseries{exception stack frames}} (E\+S\+Fs). Each thread should have its own exception stack. When a T\+R\+Y\+\_\+\+W\+I\+T\+H(...) block begins, a new E\+SF is pushed to the stack, and the block starts to execute.

Each E\+SF has two lists of functions of type @ exception\+\_\+handler, which is defined as {\ttfamily void ($\ast$)(int)}. The nodes for these lists are {\ttfamily struct \mbox{\hyperlink{structexception__handler__frame}{exception\+\_\+handler\+\_\+frame}}} objects. Initially, the new E\+SF has empty lists. The first list is the list of {\bfseries{catchers}} and the second is the list of {\bfseries{finalizers}}.

As the T\+R\+Y-\/block executes, execution reaches {\ttfamily F\+I\+N\+A\+L\+L\+Y()} and {\ttfamily O\+N\+\_\+\+E\+R\+R\+OR} blocks. When a {\ttfamily F\+I\+N\+A\+L\+L\+Y()} block is reached, a new handler is added to the finalizers list. When a {\ttfamily O\+N\+\_\+\+E\+R\+R\+OR} block is reached, a new handler is added to the catchers list.

If execution arrives at the end of the T\+R\+Y-\/block, the list of catchers is thrown away and the finalizers are executed (in reverse order, that is, last-\/in-\/first-\/out).

If at some point the function {\ttfamily raise\+\_\+exception()} is called, execution jumps back at the T\+R\+Y-\/block at the top of the exception stack. There, each catcher is first executed, followed by all the finalizers. At the end, the E\+SF is popped from the exception stack. Then, if at least one catcher did execute, the exception is considered handled, and execution continues after the T\+R\+Y-\/block. If however there was no catcher executed, and the exception stack is non-\/empty, then {\ttfamily raise\+\_\+exception()} is called again, to repeat the process.

An exception stack is defined simply as a pointer to {\ttfamily struct \mbox{\hyperlink{structexception__stack__frame}{exception\+\_\+stack\+\_\+frame}}}. An \+\_\+\+\_\+exception context\+\_\+\+\_\+is a pointer to such a pointer, that is, 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{typedef} \textcolor{keyword}{struct }\mbox{\hyperlink{structexception__stack__frame}{exception\_stack\_frame}}** \mbox{\hyperlink{structexception__stack__frame}{exception\_context}};}
\end{DoxyCode}
 A context needs to be available to our code in two places\+: when a {\ttfamily T\+R\+Y\+\_\+\+W\+I\+T\+H(context)} block is defined, and when {\ttfamily raise\+\_\+exception(context)} is called.

One can simply define a context as a global variable\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{comment}{// at top level}}
\DoxyCodeLine{\textcolor{keyword}{struct }execution\_stack\_frame* exception\_stack = NULL;}
\DoxyCodeLine{\textcolor{preprocessor}{\#define TRY  TRY\_WITH(\&exception\_stack)}}
\DoxyCodeLine{\textcolor{preprocessor}{\#define RAISE  raise\_exception(\&exception\_stack)}}
\DoxyCodeLine{}
\DoxyCodeLine{TRY \{}
\DoxyCodeLine{    ...}
\DoxyCodeLine{    RAISE;}
\DoxyCodeLine{    ...}
\DoxyCodeLine{\}}
\end{DoxyCode}


In a multi-\/threaded case, it is necessary to declare one context for each thread. This can be done at the T\+CB, for example.


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{struct }\mbox{\hyperlink{structthread__control__block}{TCB}} \{}
\DoxyCodeLine{    ....}
\DoxyCodeLine{    \textcolor{keyword}{struct }execution\_stack\_frame* exception\_stack = NULL;   }
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{preprocessor}{\#define TRY  TRY\_WITH(\& CURTHREAD-\/>exception\_stack)}}
\DoxyCodeLine{\textcolor{preprocessor}{\#define RAISE  raise\_exception(\& CURTHREAD-\/>exception\_stack)}}
\end{DoxyCode}
\hypertarget{group__exceptions_autotoc_md29}{}\doxysubsubsection{Performance}\label{group__exceptions_autotoc_md29}
Although setting up a T\+R\+Y-\/block is relatively cheap (basically, a call to {\ttfamily setjmp} is done), it is better to avoid calling exception handlers. So, for very critical pieces of code, one could do 
\begin{DoxyCode}{0}
\DoxyCodeLine{TRY \{}
\DoxyCodeLine{    lock\_mutex();}
\DoxyCodeLine{    ON\_ERROR \{}
\DoxyCodeLine{        unlock\_mutex();}
\DoxyCodeLine{    \}}
\DoxyCodeLine{}
\DoxyCodeLine{    ... \textcolor{comment}{// stuff}}
\DoxyCodeLine{}
\DoxyCodeLine{    unlock\_mutex();}
\DoxyCodeLine{\}}
\end{DoxyCode}
 instead of the more convenient 
\begin{DoxyCode}{0}
\DoxyCodeLine{TRY \{}
\DoxyCodeLine{    lock\_mutex();}
\DoxyCodeLine{    FINALLY(e) \{}
\DoxyCodeLine{        unlock\_mutex();}
\DoxyCodeLine{    \}}
\DoxyCodeLine{}
\DoxyCodeLine{    ... \textcolor{comment}{// stuff    }}
\DoxyCodeLine{\}}
\end{DoxyCode}


The first case is faster, because it avoids a function call, when there is no exception, whereas the second will make a call to the {\ttfamily F\+I\+N\+A\+L\+LY} block, even without an exception raised. But, remember\+: {\bfseries{premature optimization is the source of all evil}}.\hypertarget{group__exceptions_autotoc_md30}{}\doxysubsubsection{Raising from inside an exception handler}\label{group__exceptions_autotoc_md30}
It is perfecly legal and supported to have exceptions raised from inside {\ttfamily O\+N\+\_\+\+E\+R\+R\+OR} or {\ttfamily F\+I\+N\+A\+L\+LY} blocks (or the functions they call).

What happens in this case is that the exception handler execution is aborted and the processing continues with the next exception handler of the E\+SF. 